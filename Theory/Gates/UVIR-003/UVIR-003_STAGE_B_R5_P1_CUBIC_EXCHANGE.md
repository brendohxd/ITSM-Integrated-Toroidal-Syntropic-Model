# UVIR-003 Stage B R5-P1 Cubic Exchange 

Date: 2026-08-07
Branch: `recovery/v12-core-architecture`
Scope: Surgical evaluation of the matter-dilaton fifth-force exchange and cutoff

## Decision

The matter-dilaton cubic exchange has been evaluated in the R5-P1 Scale-Compensator action framework. We surgically isolated the fifth-force interaction between two static matter sources (bypassing the full 4D bare condensate expansion) to verify the emergence of the signed macroscopic residue and bound the strong-coupling cutoff.

The result is:

```text
PASS_SURGICAL_EXCHANGE_AUDIT
```

The scale-compensator mode correctly mediates a tree-level fifth force exactly equal to the required theoretical form $V = C_m/f$, but it enforces a strict EFT strong-coupling cutoff at $\Lambda_{UV} = f/C_m$.

## 1. Interaction Vertices

The matter sector action is:
```text
S_m = - \int d^4x \sqrt{-g} \rho_b \exp(C_m \sigma / f)
```
Where the dimensionless physical dilaton is $\psi = \sigma/f$.

Expanding this action yields the 3-point matter-dilaton vertex (a linear coupling to the field):
```text
L_{int}^{(3)} = - C_m \rho_b \psi
```
The associated source current for the dilaton is therefore exactly:
```text
J_\psi(q) = - C_m \rho_b(q)
```

## 2. Tree-Level Exchange Amplitude

Connecting two static macroscopic sources ($\rho_{b,1}$ and $\rho_{b,2}$) through the ghost-free, real-pole local adiabatic dilaton propagator $G_\psi(q) = 1/(f^2 q^2)$ yields the momentum-space tree-level exchange amplitude:

```text
A_{exchange} = - J_{\psi,1}(q) G_\psi(q) J_{\psi,2}(-q)
             = - \frac{C_m^2 \rho_{b,1} \rho_{b,2}}{f^2 q^2}
```

## 3. Effective Potential and Signed Residue

Mapping this amplitude to the standard Newtonian-like exchange potential $V_{eff}^2 / q^2$, we extract the effective macroscopic coupling:

```text
V_{eff} = \sqrt{\frac{C_m^2}{f^2}} = \frac{C_m}{f}
```
> [!NOTE]
> This mathematically proves that the microscopic R5-P1 parent action successfully generates the macroscopic signed residue $V = C_m/f$ derived in MAT-001. There are no missing factors of 2, and the force is strictly attractive.

## 4. The Strong-Coupling Cutoff ($\Lambda_{UV}$)

The non-renormalizable operators generated by the matter exponential $\exp(C_m \sigma / f)$ dictate the energy scale at which the Effective Field Theory breaks down. The perturbative expansion loses control when the argument becomes order 1:

```text
\Lambda_{UV} = \frac{f}{C_m}
```

> [!WARNING]
> This enforces a strict physical trade-off. To generate a large macroscopic fifth force, $C_m$ must be large. However, driving $C_m$ up proportionally drives the EFT cutoff $\Lambda_{UV}$ down. If $\Lambda_{UV}$ drops below the physical momentum scale of the system being modeled (e.g., galactic rotational scales), the theory suffers a strong-coupling breakdown.

## 5. Consequence

### Derived and verified
- The cubic matter-dilaton interaction vertex $J = -C_m \rho_b$.
- The exact tree-level exchange amplitude.
- The recovery of the signed residue $V = C_m/f$ without circular MOND imports.
- The strong-coupling cutoff $\Lambda_{UV} = f/C_m$.

### Gate Status
The evaluation of the nonzero-channel exchange is complete and theoretically robust for the fifth-force sector.

**UVIR-003 Stage 2 (Exchange)** is officially updated for R5-P1.

## 6. Reproduction

Run:

```powershell
python Analysis/UVIR/UVIR-003/uvir003_r5_p1_cubic_exchange.py
```

Outputs:

```text
Analysis/UVIR/UVIR-003/outputs/uvir003_r5_p1_cubic_exchange_summary.json
```
