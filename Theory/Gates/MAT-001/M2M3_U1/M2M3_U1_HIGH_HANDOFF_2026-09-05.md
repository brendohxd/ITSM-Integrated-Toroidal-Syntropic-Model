# M2/M3-U1 High-to-Max handoff

**Date:** 2026-09-05  
**Completed:** H-1 action ledger; H-2 literature delta  
**Disposition:** `READY_FOR_MAX_DERIVATION_SCOPED_CONTROLS`  
**Gate effect:** none  

## Frozen controls

1. `RCP-C0`: exact published fixed-background invariant-portal comparator.
2. `RCP-I1-T`: literal trace-operator EFT with `T_m^(0)` defined before the
   portal.
3. `RCP-I1-C`: universal conformal-matter-metric action class with `A(s)` and
   its background derivatives explicit.
4. `RCP-PKM1`: separate existing metric-hosted control; no action mixing.

## Max tasks authorized by the plan

- reproduce RCP-C0 background, amplitude-phase kernel, dispersions, `P(X)` and
  GP reductions;
- vary and reduce RCP-I1-T and RCP-I1-C in their scoped fixed-background or
  on-shell domains;
- derive linear/static source response, healing length and nonlinear spherical
  charge;
- test both signs, limits, dimensions and field-chart maps;
- report every free invariant coefficient.

## Not yet authorized scientifically

- full coupled amplitude-phase-metric constraint elimination;
- U5 presymplectic/ADM reconciliation;
- M4 physical pole-residue decision;
- PPN, lensing or GW claims;
- topology/winding normalization;
- `a_0` comparison or observational fitting;
- gate promotion.

Those belong to the Ultra plan only after Max establishes a non-empty healthy
domain.

## Required mode change

Switch from **High** to **Max** before executing the authorized algebra. Stop
and return to High/Medium for report and hash work after the bounded reduction.

