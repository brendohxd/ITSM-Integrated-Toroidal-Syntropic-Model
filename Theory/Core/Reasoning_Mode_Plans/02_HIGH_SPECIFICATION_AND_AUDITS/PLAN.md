# High plan — route specification and hostile audits

**Required reasoning:** High  
**Gate effect:** none  
**Can start:** immediately  
**Hard boundary:** no full coupled constraint reduction or gate-critical pole calculation  

## Purpose

Freeze the questions, action classes, definitions, provenance and falsification
criteria needed before expensive mathematics. High is also appropriate for
referee-style code-to-claim inspection and interpreting already-computed
evidence.

## H-0 — authority re-anchor

Read in order:

1. `GEMINI.md`;
2. `Theory/Core/ITSM_CORE_IDENTITY_BRIEFING.md`;
3. `active_research.md`;
4. the parent route/publication plan;
5. current MAT, UVIR, VOR, CBR and RES parent decisions relevant to a selected
   work package.

Record current status; do not normalize a conflict by choosing the most
optimistic document.

## H-1 — register and freeze `M2/M3-U1`

For `M2/M3-U1_CONDENSATE_PORTAL_CONTROL`:

1. Define the three namespaces separately: literature comparator `RCP-C0`,
   ITSM-compatible vacuum-condensate candidate `RCP-I1`, and metric-hosted
   `RCP-PKM1`.
2. Write one frozen covariant action or explicit incomplete action for each.
3. Define the schematic `F(|Phi|^2) T_m` portal off shell. Resolve whether
   `T_m` means the trace derived from a specified matter action or another
   scalar matter operator; avoid circular stress-tensor variation.
4. Inventory fields, dimensions, signs, symmetries, background ensemble,
   source convention and parameter provenance.
5. State exactly which terms are present in the action and which are expected
   to emerge after expansion.
6. Prohibit observational values from entering the derivation.
7. Predeclare positive, conditional, hold and no-go outcomes.

Deliverable: RCP-0 action ledger and frozen route specification.

## H-2 — literature-to-ITSM delta audit

Audit the 2026 *Superfluid Dark Matter* review and the 2019 invariant-portal
paper as external precedents. Record:

- exact parent action and finite-density solution;
- assumptions used in the relativistic-to-GP and `P(X)` reductions;
- amplitude-phase mixing and source terms;
- healing-length and nonlinear-screening assumptions;
- inverse-square rather than MOND-like force behavior;
- free normalization combination such as `v/Lambda^2`;
- sign/gradient-instability, equivalence-principle and lensing risks;
- the ontology difference between particle dark matter and the ITSM active
  vacuum.

Deliverable: claim-level delta table. Literature precedent cannot change a
repository gate.

## H-3 — P2 hostile scientific audit

Using the fresh Medium evidence where available:

- inspect dimensions, signs, boundary conditions and regulator logic;
- trace every paper claim to code and raw output;
- verify that the tested diagnostic distinguishes crossing, residence time,
  plateau and attractor;
- inspect sensitivity to initial conditions and numerical settings;
- confirm the free-field/biaxial scope;
- audit title, abstract, figures and conclusion against current canonical
  statuses rather than stale selective-publishing text.

Deliverable: severity-ranked hostile report and a list of calculations that
require Max-level independent reproduction.

## H-4 — winding and phase-normalization specification

- fix whether `Theta` is dimensionless;
- record all `hbar` and `c` conventions;
- define Noether charge, physical velocity and circulation as distinct maps;
- define each compact length as edge, geodesic cycle, circumference or modulus;
- preserve the corrected preregistered S2-T02 failure;
- separate a winding integer from a coupling or acceleration scale;
- define the exact parent bridge required before joining VOR to UVIR/MAT.

Deliverable: convention/provenance ledger and Max calculation specification.

## H-5 — reservoir scaffold claim audit

Read the present RES-001 action, code and outputs and separate:

- mathematical GKSL properties actually demonstrated;
- phenomenological jump operators and rates;
- assumed microscopic Hamiltonian data;
- unproved identification with a covariant `Q^mu`;
- any use of total Bianchi conservation as though it fixed constituent
  exchange currents.

Deliverable: corrected entry requirements for the future Ultra reservoir plan.

## H-6 — route decision and handoff

For each package classify:

- `READY_FOR_MEDIUM_REPRODUCTION`;
- `READY_FOR_MAX_DERIVATION`;
- `HOLD_INCOMPLETE_ACTION`;
- `REJECT_SCOPED_CLASS`.

Name the exact next file, equation and missing input. Do not use a generic
`PASS` or `CLEARED` label.

## Stop and switch to Max when

- an action and background convention are frozen;
- the task requires deriving coupled amplitude-phase equations, integrating a
  heavy mode, solving nonlinear source profiles, or independently reproducing
  the Casimir mathematics.

## Completion record

Report the frozen action hashes, audit dispositions and Max-ready work packages.
End the task without beginning their algebra.

Completed 2026-09-05. See
`HIGH_COMPLETION_RECORD_2026-09-05.md`. No gate was promoted.
