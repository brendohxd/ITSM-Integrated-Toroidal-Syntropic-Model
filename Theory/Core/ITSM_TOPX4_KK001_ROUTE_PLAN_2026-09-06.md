# ITSM TOP-X4 / KK-001 higher-dimensional route plan

**Date:** 2026-09-06
**Status:** `ACTIVE_RESEARCH_FORK`; no physics-gate effect
**Operator priority:** redirect new derivation work to this route until its next
decision boundary
**Canonical effect:** none; the retained ITSM spatial identity remains `T^3`
unless a later, separately authorized architecture decision replaces it

## 1. Route question

Test whether a closed higher-dimensional parent on

\[
  \mathcal M_5=\mathbb R_t\times\mathbb T^3_{\rm obs}\times S^1_y
\]

can reduce to a healthy four-dimensional finite-density condensate theory and
provide a microscopic interpretation of presently open ITSM structures,
especially compact moduli, hidden excitations and effective reservoir exchange.

This is a controlled extension, not a claim that observed spacetime has
periodic Lorentzian time. It must not be called `Route T4`, because TOP-001
already uses that label for twisted `E2/E3` flat three-manifolds.

## 2. Fixed geometric convention

- Observable coordinates are `x^mu`, with spatial slices `T^3`.
- The additional coordinate is dimensionless, `y ~ y + 2*pi`.
- Its physical radius is `R(x)` and circumference is `ell_4=2*pi*R`.
- For fixed rectangular geometry,

\[
 ds_5^2=g_{\mu\nu}(x)dx^\mu dx^\nu+R^2dy^2.
\]

- A bulk field is expanded as

\[
 \Phi(x,y)=\ell_4^{-1/2}\sum_{n\in\mathbb Z}
 \phi_n(x)e^{iny},
 \qquad
 m_n^2=m_0^2+\frac{n^2}{R^2}
 =m_0^2+\left(\frac{2\pi n}{\ell_4}\right)^2.
\]

No observed acceleration, Hubble value, galaxy fit, `2*pi` target or desired
reservoir law may determine `R`, `ell_4`, a coupling or a potential.

## 3. Why the route could matter

From the four-dimensional effective perspective, the parent can contain:

1. a radion/modulus associated with `R(x)`;
2. a Kaluza--Klein tower of condensate and metric excitations;
3. four independent winding sectors on spatial `T^4`;
4. Casimir and condensate contributions to a radion effective potential;
5. apparent exchange between retained and traced-out mode sectors while the
   complete five-dimensional stress tensor remains conserved.

The fifth item is only a research opportunity. Compactness alone does not
produce a nonzero `Q^mu`, and integrated flux around a closed circle cannot be
relabeled as energy entering through an external boundary.

## 4. Namespace and non-mixing rule

| Namespace | Meaning | Present status |
|---|---|---|
| `TOP-X4` | Geometric and spectral four-torus control | Active bounded fork |
| `KK-001` | Dimensional reduction, radion and KK-mode physics | Original control held at D2; one S2F3 retry frozen |
| `X4-C0` | Free fixed-background scalar control | A0 only |
| `X4-I1C` | Frozen interacting finite-charge bulk control | D2 hold: radion unstabilized |
| `X4-S2F3` | `X4-I1C` plus three periodic massive 5D Dirac spectators | Frozen for one Max retry only |
| `X4-R1` | Candidate reduced open-EFT/reservoir description | Dependency locked |

Equations, coefficients and passes cannot be inherited between namespaces.
Bulk matter, brane-localized matter and an inserted four-dimensional portal are
different theories and must never be combined implicitly.

## 5. Staged programme

### A0 — exact kinematic and dimensional control (High)

Calculate without observational inputs:

- `T^4` homology/Betti counts and Euler characteristic;
- direct four-torus Laplacian spectrum versus `T^3 + KK` decomposition;
- isotropic first-shell degeneracies;
- radius/circumference normalization and a deliberate wrong-normalization
  negative control;
- five- to four-dimensional field normalization;
- mass dimensions of `M5`, `M_Pl`, the scalar, quartic coupling and winding
  energy;
- small-circle decoupling and large-circle/decompactification trends.

Pass meaning: the declared convention is internally consistent. It is not a
physics pass and does not select a compactification radius.

### A1 — frozen parent and background (High)

Write one complete five-dimensional off-shell action, including gravity,
condensate potential, matter location, boundary conditions and every coupling.
Derive the finite-density background and radion equation before perturbing.

Required choices:

- bulk or brane matter;
- global or gauged condensate `U(1)`;
- state/ensemble and chemical-potential implementation;
- background winding or zero winding;
- classical and quantum contributions to radion stabilization.

Kill if the background is off shell, the matter definition is circular, or a
parameter is selected from `a0`, `H0`, SPARC or another desired result.

### A2 — fixed-background dimensional reduction (Max)

Derive the complete quadratic KK tower and four-dimensional action. Verify

\[
 M_{\rm Pl}^2=\ell_4 M_5^3,
\]

canonical mode normalization, zero-mode recovery, KK momentum/selection rules,
the small-circle EFT, the decompactification limit and the cutoff domain.

Kill on a ghost, tachyon without a declared condensate interpretation,
singular zero-mode limit, or no separation between the KK gap and EFT cutoff.

### A3 — radion, winding and Casimir stability (Max)

Calculate the radion kinetic term and the complete declared effective
potential. Include condensate winding and a properly renormalized compact-space
stress only under explicit field content, state and boundary conditions.

Required result: a stable or metastable stationary radius with positive radion
mass and a lifetime/domain compatible with the reduction. An unfixed radius is
a hold, not permission to choose the required scale.

### A4 — effective exchange and source response (Max)

Partition zero and excited modes from the same action. Derive any transfer
current from interactions or an influence functional and demonstrate

\[
 \nabla_\mu T_0^{\mu\nu}=Q^\nu,
 \qquad
 \nabla_\mu T_{\rm hidden}^{\mu\nu}=-Q^\nu.
\]

Show that the nested no-interaction state has `Q^nu=0`. Bianchi conservation
alone cannot determine the split current. Compute the signed matter response;
do not identify a bare reduction coefficient with physical `K_Q` or `V`.

### A5 — coupled metric/radion/condensate constraints (Ultra)

Only after A1--A4 survive: perform the five-dimensional ADM/Dirac count and the
four-dimensional reduced constraint analysis. Calculate physical poles,
residues, characteristics, strong-coupling scales and field-redefinition
invariants. Test zero-density, zero-coupling, zero-winding, small-circle and
decompactification limits.

### A6 — relativistic and observational decision (High/Ultra as required)

Only a survivor may enter PPN, equivalence-principle, lensing, gravitational-
wave, cosmological and compact-topology tests. Observations test the frozen
prediction; they do not set `R` or repair the parent.

## 6. Decision gates

| Decision | Advance | Hold | Freeze/reject |
|---|---|---|---|
| X4-D0 | A0 identities and dimensions all pass | Convention ambiguity remains | Compact Lorentzian time or inconsistent normalization |
| X4-D1 | Complete off-shell action and on-shell background | Isolated missing operator | Hybrid action or target-derived coefficient |
| X4-D2 | Healthy KK/radion spectrum with EFT hierarchy | Radius unstabilized | Ghost, singular reduction or no cutoff overlap |
| X4-D3 | Nonzero derived sector exchange with nested null limit | Only phenomenological closure | Bianchi identity used as source law or conservation failure |
| X4-D4 | Physical matter/metric residues and acceptable local limit | Free normalization remains | Wrong sign, equivalence violation or excluded fifth force |

## 7. Current work suspension

While `TOP-X4 / KK-001` is the operator-selected priority:

- preserve all P2, MAT, UVIR, VOR and RES artifacts and statuses;
- do not dispatch their queued external packages;
- do not open new downstream SPARC, cosmology, lensing or publication work;
- allow only integrity repairs necessary to prevent data loss or false status;
- reassess the wider architecture only at an X4 decision boundary.

## 8. Reasoning and cost boundaries

- **High complete:** plan 10 selected and froze `X4-S2F3`, consisting of three
  periodic massive 5D Dirac spectators added to `X4-I1C`.
- **Max next:** plan 11 must independently calculate the determinant and
  finite-charge semiclassical background; do not continue A4 from `X4-I1C`.
- **Ultra closed:** A5 still requires a healthy reduced parent, stable radion
  and derived nonzero exchange.
- **Medium:** deterministic reruns, hashes and packaging.

## 9. Current status

The original parent remains `X4-D2 HOLD_UNSTABILIZED`. A0 and A1 remain valid bounded control results. In
Max A2/A3, the independent Einstein-frame reduction passed 6/6 identities and
the fixed-action audit completed 15/15 registered calculations, but the exact
classical static-circle condition failed, the A1 radius ran, the massless
periodic-boson Casimir balance was a maximum, and no EFT hierarchy was
established. A4 and Ultra entry are closed. This changes no physics-gate
status, canonical architecture, manuscript claim or ITSM prediction.
The completed High prescreen found a conditional classical winding minimum
only on a changed `AdS4 x S1`, zero-temporal-charge background. It selected
`X4-S2F3` as the single Max retry after an 8/8 algebraic/sign prescreen; that
count is not a physics pass. The retry is frozen in
`Theory/Gates/TOP-X4/TOPX4_S2F3_PARENT_FREEZE_2026-09-06.md`. The only open
continuation is
`Theory/Core/Reasoning_Mode_Plans/11_MAX_TOPX4_S2F3_SEMICLASSICAL_STABILIZATION/PLAN.md`.

## 10. 2026-09-09 Plan 11 checkpoint

The first bounded Plan 11 job is complete: the zero-density static parity-even
determinant checkpoint passed `12/12` calculation checks and reproduced the
declared `N_F=3` stationary witness. Its status is
`PASS_STATIC_PARITY_EVEN_DETERMINANT_HOLD_PARITY_ODD_AND_FINITE_CHARGE`, with
`physics_pass=false` and no gate effect.

The finite-charge entry gate then passed `9/9` provenance and policy checks but
returned `HOLD_TOPX4_S2F3_BEFORE_FINITE_CHARGE_QUANTUM_COMPLETION`. The missing
finite-charge determinant/stress, evolving-state convergence, parity-odd
anomaly/counterterm audit and coupled physical Hessian remain explicit holds.
The static solver is not reused as a dynamic-background solver. A4 and Ultra
remain closed, and the three-way Rule-9 review is incomplete because Roles B
and C returned usage-limit errors without reports.
