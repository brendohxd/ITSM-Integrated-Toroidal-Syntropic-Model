# ITSM Core 12.0-alpha.3

Release date: 2026-07-26

This release freezes the nonlinear scalar-readiness checkpoint after:

- completing the evolving-FRW finite-wavenumber scalar ADM constraint
  reduction and its 48,861-point representative kinetic scan;
- identifying the strict homogeneous rank-loss direction as the background
  time-translation gauge orbit;
- deriving the complete three-dimensional flat-decoupling khronon cubic and
  quartic bases, including the 96-monomial quartic expansion;
- proving the elastic contact form, exact vanishing of elastic `t/u` cubic
  exchange and the homogeneous-gauge `s`-channel hold;
- fixing the cubic and quartic lapse/shift constraint-order identities; and
- verifying nonlinear ADM action provenance for the exact
  `gravity+aether+condensate+alignment` parent block.

The full cosmological quadratic constraint source `J2` is not yet derived.
The force regulator still needs a generally covariant evolving-frame
completion, and the non-analytic `Y^(3/2)` operator needs a controlled
prescription or smooth completion at the zero-gradient background. The
physical `2-to-2` amplitude, unitarity cutoff, full UVIR-003 gate and MAT-001
therefore remain open.

The release source carries its own `sections/` tree and does not depend on the
mutable working sections.
