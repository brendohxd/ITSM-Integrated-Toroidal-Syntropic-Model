\clearpage
\section{Dependency-ordered closure gates}

\begin{center}
\refstepcounter{table}
\small
\textbf{Table \thetable: Research gates required before restoration of strong predictions.}\par\smallskip
\begin{tabular}{@{}p{0.20\textwidth}p{0.25\textwidth}p{0.48\textwidth}@{}}
\toprule
Gate and status & Question & Pass condition \\
\midrule
UVIR-001\\\textit{Closed negative} & Does the minimal condensate yield spatial $Y^{3/2}$? & Candidate rejected; replacement route required \\
UVIR-002\\\textit{Closed provisional} & Which replacement architecture survives initial checks? & Two-sector preferred-frame route selected for testing \\
UVIR-003\\\textit{In progress} & Is the selected action stable, causal and weakly coupled? & Stage~5 holds tier-1 closure (M2/M3/M6/M7); matched $V$ then Stage~4A and re-review \\
MAT-001\\\textit{Blocked} & What is the normalized matter vertex? & Conditional Track-A form kit only; $V$ not computed; parent $Z_\phi,g_\phi$ incomplete \\
SCR-001\\\textit{Open} & Is the force screened at high gradient? & Ephemeris and binary constraints satisfied under derived universal coupling \\
LEN-001\\\textit{Open} & What metric do matter and light see? & PPN, lensing and wave propagation derived downstream of $V$ \\
DISK-001\\\textit{Methods package} & What is the exact disk response? & Validated Picard solver; raw SPARC $\chi_\nu^2 = 38.96$, median $\chi_\nu^2 = 10.51$; STAT-001 quarantined \\
TOP-001 / CBR-002\\\textit{Scoped Negative} & How do shape moduli evolve? & Free Casimir dilutes ($u_\sigma \to 10^{-6}$); driven $13/12$ is an unstable toy model \\
VOR-001\\\textit{Open scaffold} & How is global circulation carried? & Bogoliubov acoustic phonons; macroscopic circulation matching open \\
WAK-001\\\textit{Open scaffold} & Can wakes retain causal memory? & Kinematic ballistic toy; 3D fluid hydrodynamics, shocks, and lensing open \\
RES-001\\\textit{Open scaffold} & What defines reservoir throughput? & Toy Lindblad model; microscopic Hamiltonian and genuine Spohn functional open \\
ASTRO-001\\\textit{Open toy} & How does gravity modify Jeans fragmentation? & Excursion set toy with clipped parameters; non-linear star formation and IMF open \\
COS-001 / PERT-001\\\textit{Proxy / Open Tension} & What are background and perturbations? & $\Lambda\mathrm{CDM}$ sound horizon calibration; hard-coded $\sigma_8$ quarantined; Boltzmann hierarchy open \\
\bottomrule
\end{tabular}
\end{center}

The dependency order is
\begin{align}
 \mathrm{UVIR\mbox{-}001}&\to\mathrm{UVIR\mbox{-}002}
 \to\mathrm{UVIR\mbox{-}003}\to\mathrm{MAT\mbox{-}001}\nonumber\\
 &\to(\mathrm{SCR},\mathrm{LEN})\to\mathrm{DISK}\nonumber\\
 &\to(\mathrm{TOP},\mathrm{VOR},\mathrm{WAK},R)
 \to\mathrm{CBR\mbox{-}002}\nonumber\\
 &\to(\mathrm{COS},\mathrm{PERT}).
\end{align}

The frame-sector literature substitution, zero-gradient force-block
factorization, ADM readiness audit and longitudinal IR NDA estimate are
progress within UVIR-003, not new closed gates.  The force block contains one
nonnegative $z=2$ scalar in the declared zero-gradient truncation, while a
rescaling audit shows that standalone $K_Q$ is not identifiable before
physical field normalization.  The declared finite-density Minkowski
background is off shell unless a non-vacuum support sector is supplied; its
scalar response must be included before the remaining lapse and shift
constraints are eliminated.  The minimal support screen rejects vacuum
energy, a healthy two-derivative $P(X)$ scalar and the ghost-condensate point
as exact Minkowski completions; rigid support is decoupling-only.  An evolving
flat-FRW minisuperspace system and a representative on-shell branch are now
verified.  A first aether-unitary ADM subgate eliminated the principal lapse
and scalar-shift constraints and independently recovered the exact spin-0
aether speed.  The complete quadratic finite-$q$ reduction has now also
eliminated the constraints along that branch.  Its 48,861 sampled
nonzero-wavenumber kinetic matrices have three positive and no negative
eigenvalues.  The exact determinant is proportional to $q_{\rm phys}^2$, but
the follow-on endpoint audit proves that the collapsing direction is
$(H,\dot\rho,\mu)$, the homogeneous time-translation orbit.  The two
independent gauge-invariant matter scalars retain a positive regular $q=0$
kinetic block across the representative trajectory.  Thus the rank loss is a
gauge endpoint, not a physical ghost or a third homogeneous scalar.  The full
three-dimensional flat-decoupling khronon cubic and quartic operator bases are
now derived.  The quartic expansion contains 96 monomials and reduces to an
independent longitudinal calculation.  Its elastic contact term is finite,
while elastic $t/u$ cubic exchange vanishes exactly.  The centre-of-mass
$s$ channel carries zero spatial momentum and is the non-invertible homogeneous
khronon gauge orbit.  Quartic reduction also requires the second-order
constraint Schur complement, although third-order constraint solutions cancel.
The exact nonlinear $g+U+\Phi+$alignment ADM parent block reproduces the FRW
and finite-$q$ quadratic constraint system.  Track A now adopts the rest-space
Laplacian and retains exact $Y^{3/2}$ for a declared local nonzero-gradient
force analysis.  Its homogeneous force action is verified through direct
quartic order.  Its origin-linear $J_2$ is a verified component, while the
complete dressed source is $S_2=\partial_zL_3[x,z_1]$.  The generic
gravity--aether and condensate dressing, complete $S_2$, generic
$L_4[x,z_1]$ contact and corrected reduced quartic functional are assembled
at $q_{\rm phys}>0$.  A regular basis $(\Xi,Q_\rho,Q_\chi)$ has a finite
positive low-wavenumber kinetic limit over the validated representative
domain and excludes the exactly homogeneous $\Xi$ gauge mode.  The
time-dependent leg-wise projection map is fixed.  The complete analytic cubic
and reduced quartic functionals are now polarized into factorized finite-$q$
physical-basis kernels.  The quartic construction
includes the complete two-leg source and all three constraint Schur pairings.
At exact zero internal momentum, audited projectors remove
$\Sigma=-D^2\beta$ and the homogeneous $\Xi$ time-translation orbit while
retaining the lapse constraint and $(Q_\rho,Q_\chi,\Pi)$.  This rule is not a
naive $q\to0$ substitution.  The finite-$q$ physical inverse quadratic kernel
and separate exact-$q=0$ projected response are now constructed.  Positive
kinetic inertia, nonsingular constraints, inverse closure and positive
residues for sampled real positive-frequency poles are verified.  The
$q/H=100$ subset is a clean four-mode real-pole regime.  The required
fixed-comoving follow-on restores coefficient derivatives and Hubble dilution
and yields converged kinetic-normalized transfer matrices.  Frozen-pole
exponentiation is invalid in the nonadiabatic domain.  The trajectory beginning
at $q/H=0.01$ still has a maximum normalized phase-space gain of
$1.38\times10^{27}$.  Five nearby mode-resolved cases all have an
$\Xi$-seeded maximizing initial vector, but all enter an off-axis complex
quartet whose real invariant space is rank four.  The nominal pole pairs
therefore have no unique continuous real rank-two split through that interval.
The follow-on retarded-response audit instead applies generalized impulses and
readouts only in retained $(Q_\rho,Q_\chi)$, with no direct $\Xi$ or
homogeneous time-translation source support.  All five cases retain amplified
through-quartet response from $2.68\times10^{17}$ to
$9.76\times10^{19}$.  This passes the bounded gauge-source attribution
subcalculation but is not an all-background instability theorem.  The
factorized Track-A force mode remains in the full finite-$q$ framework outside
the coupled quartet block.  A controlled sampled exchange domain now exists
on the representative branch: initial $q/H=47.5,50,75,100$ passes the
real-pole, subhorizon and $0.1$ adiabatic criteria, whereas $q/H=45$ fails the
adiabatic threshold.  Every nonzero internal channel must pass independently,
and exact $q_K=0$ retains its separate projector.  Across 48 admitted
equilateral mode/sign cases, two residue-normalized on-shell legs now produce
finite nonzero cubic pair-source covectors and finite inverse-kernel
responses, with permutation and inverse closure below $6.2\times10^{-16}$.
On a regular-tetrahedral elastic slice, all three internal channels are
strictly nonzero and independently admitted.  Across 24 mode-pair cases,
matched exchange, analytic quartic contact and all constraint-Schur pairings
give finite, real and permutation-consistent local four-leg kernels.  Their
cancellation ratio reaches $0.00502$, so robustness beyond this slice remains
open.  This is not a cosmological S-matrix normalization.

Post--alpha.9 UVIR-003 working subgates (recorded at $12.0$-alpha.10, not full
gate closure) extend that slice without promoting packaging.  Off-tetrahedral
kinematics and a local adiabatic packet proxy of $K(q)$ are declared, not as
an S-matrix.  An FRW in-in \emph{path} attaches the packet proxy to high-$q$
transfer diagnostics; multi-slice $K_{\mathrm{nn}}(t)$ and a causal
mode-projected Green proxy $G_{\mathrm{mp}}(t_{\mathrm{out}},t_{\mathrm{in}})$
are assembled on the controlled high-$q$ subset.  Track~A local expansion of
exact $|\nabla\pi|^3$ about a nonzero background gradient $v>0$ yields a
positive Hessian for $A_{\mathrm{IR}}>0$ and a local cubic force vertex; the
homogeneous zero-gradient Taylor kernel remains unavailable.  A
\emph{declared} scoped unitarity/EFT criterion package combines a tree
s-wave proxy against the redefinition-invariant
$\Lambda_\parallel=K_Q^{3/4}/\sqrt{A}$ with high-$q$ Green health checks.
The optical theorem, a matched physical cutoff and absolute $K_Q$ remain
open: a matching inventory lists invariants $Aq/K_Q$ and $A/K_Q^{3/2}$ and
candidate routes R1--R5 without deriving a numeric $K_Q$.  MAT-001 therefore
remains blocked.  A subsequent Stage~1 audit records only an
evidence-bounded M2 domain: the force result has analytic support for
$A_{\rm IR}>0$ and $v>0$ with samples at $A_{\rm IR}=1$ and
$v\in\{0.05,0.1,0.25,0.5,1,2\}$; the Green and packet diagnostics cover the
discrete set $q/H\in\{47.5,50,75,100\}$, with packet centre $q_0/H=50$ and
$\sigma_{\ln q}=0.02$; and the unmatched tree/NDA diagnostic is restricted to
$q/\Lambda_\parallel\leq0.3$ and $u_L\leq1$.  This does not certify a
continuous neighbourhood between samples or prove dynamical closure against
leakage into the excluded infrared sector.  Missing infrared-HOLD evidence or
missing quantitative support now fails the audit.  Stage~2a subsequently
finds no action-level derivation or rigorous bound for the R3 product
$Z_\psi r_\rho$ in the audited declared sources, so numeric $K_Q$ remains
undetermined.  It also distinguishes the invariant $Aq/K_Q$ from the
chart-fixed diagnostic $I_{a_0}=A a_0/K_Q$ when $a_0$ is held external.
Stages~2b and 2c have recorded a named Conditional matching floor and its
scoped diagnostics.  Stage~3 then records only provisional forms because
$V=C_m/\sqrt{K_Q}$ is not computed; Stage~4 must reopen as matched Stage~4A.
Stage~5 returns \texttt{PASS\_STAGE5\_DECISION\_HOLD\_TIER1}, not physics-gate
closure.  MAT pass and downstream Derived use remain blocked.

Parallel identity work is similarly bounded.  TOP-001 S2 and VOR-001 S2b are
mathematical or toy-parent templates with \texttt{physics\_pass: false}.
WAK-001 C2 and RES-001 R1 are decision packets with status
\texttt{NOT\_SELECTED}; all catalogued alternatives remain Open.  None of
these packets supplies a modulus action, validated winding spectrum,
microscopic wake identity, reservoir law or observable.  The representative
spin-0 cone is wider than the
metric cone.  Global multicone causality and a complete UVIR-003 pass are still
unresolved.  Likewise, running CBR-002 with a source adjusted to hold
$13/12$ would test a tuning procedure, not ITSM, and fitting the algebraic
disk law does not answer the periodic nonlinear field problem.
